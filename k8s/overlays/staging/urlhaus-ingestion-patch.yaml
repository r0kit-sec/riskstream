apiVersion: apps/v1
kind: Deployment
metadata:
  name: urlhaus-ingestion
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: urlhaus-ingestion
          env:
            - name: ENVIRONMENT
              value: staging
