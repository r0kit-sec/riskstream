apiVersion: v1
kind: Service
metadata:
  name: threatfox-ingestion
  labels:
    app: threatfox-ingestion
spec:
  selector:
    app: threatfox-ingestion
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: debug
      port: 5678
      targetPort: debug
