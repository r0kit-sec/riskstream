apiVersion: v1
kind: Service
metadata:
  name: urlhaus-ingestion
  labels:
    app: urlhaus-ingestion
spec:
  selector:
    app: urlhaus-ingestion
  ports:
    - name: http
      port: 80
      targetPort: http
