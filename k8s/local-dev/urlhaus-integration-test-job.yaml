apiVersion: batch/v1
kind: Job
metadata:
  name: urlhaus-integration-test
  namespace: local-dev
  labels:
    app: urlhaus-integration-test
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: urlhaus-integration-test
    spec:
      restartPolicy: Never
      containers:
        - name: pytest
          image: python:3.12-slim
          env:
            - name: URLHAUS_BASE_URL
              value: http://urlhaus-ingestion
          command:
            - /bin/sh
            - -c
            - pip install --no-cache-dir pytest && pytest -v /tests/test_urlhaus_ingestion.py
          volumeMounts:
            - name: test-file
              mountPath: /tests
              readOnly: true
      volumes:
        - name: test-file
          configMap:
            name: urlhaus-integration-test-code
