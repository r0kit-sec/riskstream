apiVersion: batch/v1
kind: Job
metadata:
  name: urlhaus-archive-lifecycle-integration-test
  namespace: local-dev
  labels:
    app: urlhaus-archive-lifecycle-integration-test
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: urlhaus-archive-lifecycle-integration-test
    spec:
      restartPolicy: Never
      containers:
        - name: pytest
          image: urlhaus-ingestion:local
          imagePullPolicy: IfNotPresent
          env:
            - name: S3_ENDPOINT
              value: minio:9000
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: root-user
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: root-password
            - name: S3_USE_SSL
              value: "false"
          command:
            - /bin/sh
            - -c
            - python -m pip install --no-cache-dir pytest && pytest -v /tests/test_urlhaus_archive_lifecycle.py
          volumeMounts:
            - name: test-file
              mountPath: /tests
              readOnly: true
      volumes:
        - name: test-file
          configMap:
            name: urlhaus-archive-lifecycle-test-code
