apiVersion: batch/v1
kind: Job
metadata:
  name: threatfox-integration-test
  namespace: local-dev
  labels:
    app: threatfox-integration-test
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: threatfox-integration-test
    spec:
      restartPolicy: Never
      containers:
        - name: pytest
          image: python:3.12-slim
          env:
            - name: THREATFOX_BASE_URL
              value: http://threatfox-ingestion
          command:
            - /bin/sh
            - -c
            - pip install --no-cache-dir pytest && pytest -v /tests/test_threatfox_ingestion.py
          volumeMounts:
            - name: test-file
              mountPath: /tests
              readOnly: true
      volumes:
        - name: test-file
          configMap:
            name: threatfox-integration-test-code
